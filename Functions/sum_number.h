
// This is a function of summing the integer numbers

int Sum(int number)
    {
        int sum = 0;
        for (int idx = 1;idx <= number ; idx++)
        {
            sum = sum + idx;
        }
        return sum;
    }   
